'use strict';

// Array vacio
const nuevoArray = [];
console.log(nuevoArray);

// Array con valores iniciales
const arrayCompras = ["Leche", "Carne", "Lechuga", "Maple de Huevos"];
console.log(arrayCompras);

// Acceder a los elementos por posición
const primer_elemento = arrayCompras[0];
console.log(primer_elemento);

const segundo_elemento = arrayCompras[1];
console.log(segundo_elemento);
